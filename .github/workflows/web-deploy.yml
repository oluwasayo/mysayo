name: Web Deploy

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/web-checks-reusable.yml'
      - '.github/workflows/web-deploy.yml'
      - 'code/app/shared/**'
      - 'code/app/web/**'
      - 'knip.json'
      - 'package-lock.json'
      - 'package.json'
      - 'vite.shared.js'
      - 'vitest.config.js'

jobs:
  validate-web:
    uses: ./.github/workflows/web-checks-reusable.yml
    with:
      upload_test_report: true

  deploy-web:
    environment: production
    if: always() && !cancelled() && needs.validate-web.result == 'success'
    needs: [validate-web]
    permissions:
      contents: read
      deployments: write
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          cache: npm
          cache-dependency-path: package-lock.json
          node-version: 26

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        working-directory: code/app/web
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: pages deploy code/app/web/dist --project-name=mysayo-web
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          wranglerVersion: '4.120.0'
