{
  "description": "Documentation and notes (placeholder)",
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  },
  "name": "doc",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist && find src -type f -name \"*.js\" -delete 2>/dev/null || true",
    "fix": "npm run clean && tsc && npm run lint:fix",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "tsc": "tsc"
  },
  "type": "module",
  "version": "0.0.1"
}
